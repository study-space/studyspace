CREATE TABLE PLACE (
    id number not NULL
    ,name VARCHAR(100)
    ,address VARCHAR(200)
    ,longitude NUMBER NOT NULL
    ,latitude NUMBER NOT NULL
    ,telephoneNumber VARCHAR(15)
    , PRIMARY KEY(id)
);